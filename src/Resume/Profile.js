import { Section } from "./Section";

export function Profile() {
  return (
    <Section title="Profile">
      <p>
        I’m an engineer with more than a decade of experience in web technologies looking for a remote position at a highly collaborative, product-focused company. I want to work with devs that are smarter than me in an environment that encourages growth.
      </p>
    </Section>
  );
}
